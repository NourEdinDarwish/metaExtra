# metaExtra (development version)

* New `forest_save()` saves a `meta` forest plot to disk with automatically calculated ideal dimensions from the forest plot's internal grid layout.
* New `forest_dims()` calculates the optimal width and height of a `meta` forest plot for saving.
